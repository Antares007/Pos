<UserControl x:Class="POS.Views.PriceListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Margin="2"
             Height="300" Width="300">
    <Grid>
        <Border BorderBrush="Black" BorderThickness="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Image Source="{Binding Photo}"/>
                <StackPanel Grid.Row="1">
                    <TextBlock x:Name="ItemName" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock x:Name="Skus" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock x:Name="Eans" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock x:Name="Price" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
