<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:Controls="clr-namespace:WpfKb.Controls;assembly=WpfKb"
	mc:Ignorable="d"
	x:Class="POS.Views.Sale.AddItem"
	Width="900" Height="400">

	<Grid x:Name="LayoutRoot">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
            <TextBox  x:Name="ItemCode" Text="{Binding AddItem[kodi], Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="72,8,2,0" FontSize="30"/>
			<Button Content="დამატება"
                    Command="{Binding AddItem}"
                    Grid.Column="1" 
                    Margin="2,8,8,0"
                    Padding="4,16,4,15" FontWeight="Bold" Foreground="White"
                    FontSize="23.2" Style="{DynamicResource TouchScreenButtonStyle}"
                    HorizontalAlignment="Stretch"/>
		</Grid>
		<Controls:OnScreenKeyboard Style="{DynamicResource KeyboardStyle}" Grid.Row="1"/>
	</Grid>
</UserControl>