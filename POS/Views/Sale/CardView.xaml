<UserControl x:Class="POS.Views.Sale.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid>
    	<Grid.CacheMode>
    		<BitmapCache/>
    	</Grid.CacheMode>
        <Rectangle Fill="{DynamicResource creditCard}"/>
        <Border  VerticalAlignment="Bottom" 
                 HorizontalAlignment="Right"
                 Margin="0,0,30,30" Background="#7FFFFFFF" CornerRadius="4" Padding="4">
            <TextBlock Text="{Binding CardNumber}" 
                   FontWeight="Bold"
                   FontSize="18"/>
        </Border>
        <Border VerticalAlignment="Center" 
                HorizontalAlignment="Center" Background="#66FFFFFF" CornerRadius="4" Padding="4">
            <TextBlock Text="{Binding Discount}" 
                   FontWeight="Bold"
                   FontSize="36"/>
        </Border>
    </Grid>
</UserControl>
