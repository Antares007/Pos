<UserControl x:Class="POS.Views.Sale.PaymentFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             mc:Ignorable="d" BorderBrush="Black" BorderThickness="1"
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid Background="Transparent">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="MouseLeftButtonDown">
                <i:InvokeCommandAction Command="{Binding Link}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <TextBlock x:Name="Name" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        <TextBlock x:Name="Value" VerticalAlignment="Center" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
